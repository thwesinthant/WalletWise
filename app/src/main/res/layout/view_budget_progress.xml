<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/bg_card_white"
    android:padding="20dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tvBudgetTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ellipsize="end"
            android:fontFamily="@font/dosis_bold"
            android:maxLines="1"
            android:text="@string/budget_progress"
            android:textColor="@color/text_primary"
            android:textSize="16sp" />

        <TextView
            android:id="@+id/tvBudgetPeriod"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:fontFamily="@font/dosis_regular"
            android:textColor="@color/text_muted"
            android:textSize="11sp"
            tools:text="Sep 1 - Sep 30" />
    </LinearLayout>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:fontFamily="@font/dosis_regular"
        android:text="@string/budget_progress_subtitle"
        android:textColor="@color/text_secondary"
        android:textSize="13sp" />

    <!-- Overall spent-of-limit summary (hidden when there's no active budget) -->
    <LinearLayout
        android:id="@+id/budgetSummaryGroup"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvBudgetSpent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:ellipsize="end"
            android:fontFamily="@font/dosis_bold"
            android:maxLines="1"
            android:textColor="@color/text_primary"
            android:textSize="22sp"
            tools:text="MMK 42,000.00" />

        <TextView
            android:id="@+id/tvBudgetLimit"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:ellipsize="end"
            android:fontFamily="@font/dosis_regular"
            android:maxLines="1"
            android:textColor="@color/text_secondary"
            android:textSize="13sp"
            tools:text="of MMK 60,000.00 budgeted" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <com.example.walletwise.dashboard.ProgressBarView
                android:id="@+id/budgetOverallProgressBar"
                android:layout_width="0dp"
                android:layout_height="8dp"
                android:layout_weight="1" />

            <TextView
                android:id="@+id/tvBudgetOverallPercent"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="10dp"
                android:fontFamily="@font/dosis_semibold"
                android:textSize="13sp"
                tools:text="70%" />
        </LinearLayout>

        <LinearLayout
            android:id="@+id/budgetItemsContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />
    </LinearLayout>

    <TextView
        android:id="@+id/tvBudgetEmptyState"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:fontFamily="@font/dosis_regular"
        android:gravity="center"
        android:text="@string/no_active_budget"
        android:textColor="@color/text_secondary"
        android:textSize="13sp"
        android:visibility="gone" />

</LinearLayout>
